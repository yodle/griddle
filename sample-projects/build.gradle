subprojects {
  buildscript {
    dependencies {
      classpath files("${rootDir}/../releases/griddle-1.5.jar")
    }
  }

  repositories {
    mavenCentral()
    maven { url 'http://maven.twttr.com' }
  }
}